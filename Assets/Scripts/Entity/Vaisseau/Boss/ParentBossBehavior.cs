using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParentBossBehavior : ParentEntity
{
    public List<EntitySpaceShipBehavior> listEntity = new List<EntitySpaceShipBehavior>();

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
